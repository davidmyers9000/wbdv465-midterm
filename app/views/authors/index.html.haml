%h2 Authors

- if is_user_admin?
  %br
  = link_to 'New Author', new_author_path, class: "btn btn-primary"




- @authors.each do |author|

  %article.row
    .col-md-12
      %h3= link_to author.name, author
    .col-md-2
      = image_tag author.picture.url(:thumb), width: 125

    .col-md-10
      %h4= pluralize author.books.size, "Book"
      %p= author.bio

      - if is_user_admin?
        %td= link_to 'Edit', edit_author_path(author)
        \|
        %td= link_to 'Delete', author, :method => :delete, :data => { :confirm => 'Are you sure?' }



